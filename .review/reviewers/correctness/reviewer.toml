name = "correctness"
version = "1.0.0"
subjects = ["diff", "whole-tree"]

[runner]
program = "codex"
# Temporarily inherit the machine-configured Codex model. The adapter owns its
# exec, sandbox, output-schema, and stdin arguments.
args = []
