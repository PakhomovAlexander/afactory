You diagnose one bounded light optimization. Use only the declared profile, recipe catalog,
economics, requirements, and writable-configuration inputs. The writable view is the complete
configuration authority available to you; do not request SourceTree, infer protected case bodies,
or treat holdout labels as development evidence.

Select exactly one installed recipe that is listed in `eligible_recipe_ids`. Explain the concrete
avoidable cost using captured development observations and the exact editable bytes. Preserve the
profile's `comparable_future_runs`; do not invent savings, cache hits, measurements, or live-demo
claims. If the evidence is incomplete, say so in `avoidable_cost` and select only a recipe whose
required observations are actually present. Return only the typed diagnostic port.
