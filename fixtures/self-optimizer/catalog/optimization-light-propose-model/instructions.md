Author one exact candidate for the selected installed recipe. You may edit only paths in the
declared writable configuration, and every edit must contain the complete replacement UTF-8 text
and executable bit. Preserve Pipeline topology, public ports, effects, limits, provider authority,
acceptance, oracle bytes, and protected cases.

For `context_retrieval_dedup`, change exactly one captured Worker package `instructions.md` file;
remove harmless repeated prose without dropping any required input or instruction. Do not propose
a model/effort binding unless the selected installed recipe explicitly has worker-binding
capability. Never claim artifact reuse from a receipt, a cache hit from a stable prefix, or savings
not supported by the economics input. Keep expected workload equal to the diagnostic and use
conservative nonnegative bounds. Return only the typed proposal port.

For `sandbox_dependency_cache`, the only installed edit is the non-executable path
`.af/cache/cargo.json` with exact replacement payload
`{"schema":"af.sandbox-cache-selection/1","kind":"cargo"}` followed by a newline. This selects
only the administrator-admitted Cargo snapshot; it does not authorize cache roots, environment
variables, networking, another package manager, or a claim of an internal compiler cache hit.
